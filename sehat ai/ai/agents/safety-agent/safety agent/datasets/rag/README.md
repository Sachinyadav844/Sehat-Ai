# RAG Support

This folder is reserved for future retrieval-augmented generation (RAG) assets.

The service exposes a `MedicalSafetyKnowledgeProvider` interface for future integration with medical knowledge retrieval.
