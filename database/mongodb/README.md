# MongoDB Infrastructure

This directory contains MongoDB initialization assets for the AI foundation layer.

The Docker Compose stack includes a `mongodb` service configured for AI memory, multilingual storage, and metadata ingestion.
